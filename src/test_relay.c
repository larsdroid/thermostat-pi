// first check argument (arguments.h) for a pin number
// if no argument use ini file (ini_file.h) to get the pin number
// then set the pin high until CTRL C is pressed (busy wait combined with sleep 10 seconds or something)
// OR set pin low!!! (use command line args???) needed to switch oof heating in case of emergencies!!!!!
// end of story (for permanent switching see relay.c in tryouts)

int main(int argc, char const *argv[]) {
    /* code */
    return 0;
}
